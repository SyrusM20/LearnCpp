#include<iostream> // preprocessor directive, lets us use stuff from the iostream lib, such as std :: cout, would result in errors without it

int main() {
    std :: cout <<"Hello World!";   // "std :: cout" == "character output" | "<<" == displays info to the console
    return 0;                       // return something to the OS, 0 == "everything went okay!"
}